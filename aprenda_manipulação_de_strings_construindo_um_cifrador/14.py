# Passo 14
# A função print() te dá apenas uma saída no console, 
# mas funções e métodos podem ter um valor de retorno 
# que você pode usar no seu código.

# Agora atribua alphabet.find(text[0]) a uma variável chamada index. 
# Dessa forma, index irá armazenar o valor 
# retornado por alphabet.find(text[0]).


text = "hello world"
print(text.find("w"))
alphabet = "abcdefghijklmnopqrstuvwxyz"
print(alphabet.find(text[0]))
index = alphabet.find(text[0])
